services:
 db:
  image: postgres:16.3
  container_name: postgres-db-BookingRoom
  environment:
    POSTGRES_DB: BookingRoom
    POSTGRES_USER: Lucky
    POSTGRES_PASSWORD: 108099
  volumes:
    - db-data:/var/lib/postgresql/data
   
  ports:
    - "5433:5432"

volumes:
 db-data: